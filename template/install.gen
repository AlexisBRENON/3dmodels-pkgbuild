set -eu

IFS=',' read -r -a fields <<< "$(grep "$1" "${PROJECT_DIR}/libraries.csv")"
pkgname="${fields[0]}"

tpl_pkgname="$pkgname" \
    envsubst \
      '$tpl_pkgname' \
      < "${TEMPLATE_DIR}/template.install"
